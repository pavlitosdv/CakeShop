@model PiesListViewModel


<h1>@Model.CurrentCategory</h1>

@foreach (var pie in Model.Pies)
{

        //old code

        @*<div class="col-sm-4 col-lg-4 col-md-4">
                <div class="thumbnail">
                    <img src="@pie.ImageThumbnailUrl" alt="">
                    <div class="caption">
                        <h3 class="pull-right">@pie.Price.ToString("c")</h3>
                        <h3>
                            <a asp-controller="Pie" asp-action="Details" asp-route-id="@pie.PieId">@pie.Name</a>
                        </h3>
                        <p>@pie.ShortDescription</p>
                    </div>
                </div>
            </div>*@


        //New Code whcih does the same as the previous one with the use of Partial view

        //model="pie" will the @model that will be consumed in the _Pie partial view model
        <partial name="_PieCard" model="pie" />
}